# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CrowdinXibTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CrowdinXibTest
  pod 'CrowdinSDK', :git => 'https://github.com/crowdin/mobile-sdk-ios.git', :branch => '#151'
end
